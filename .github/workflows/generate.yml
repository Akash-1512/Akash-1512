# This workflow is DISABLED — delete this file from your repo.
# The README uses github-readme-stats.vercel.app directly.
# No workflow needed for stats cards.

name: DISABLED

on:
  workflow_dispatch:  # Only runs if manually triggered

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Delete this file from your repo."
